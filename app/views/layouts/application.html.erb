<!DOCTYPE html>
<html>
  <head>
    <title>RoRBlog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <p class="<%= 'bg-blue-100 border-blue-500 text-blue-700 border-l-4 p-4' if notice %>"><%= notice %></p>
  <p class="<%= 'bg-red-100 border-red-500 text-red-700 border-l-4 p-4' if alert %>"><%= alert %></p>
  <nav class="p-4 text-black font-bold shadow-md flex justify-between items-center w-full">
    <% if user_signed_in? %>
      <div class="flex gap-4">
        <%= link_to "Home", root_path, class: "hover:underline" %>
        <%= link_to "New Blog", new_blog_post_path, class: "hover:underline" %>    
      </div>
      <%# link_to "Profile", edit_user_registration_path, class: "hover:underline" %>
      <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "bg-blue-500 text-white hover:underline font-bold py-2 px-4 rounded" %>
    <% else %>
      <%= link_to "Login", new_user_session_path, class: "hover:underline" %>
      <%# link_to "Sign Up", new_user_registration_path, class: "hover:underline" %>
    <% end %>
  </nav>

  <main class="container p-2">
    <%= yield %>  
  </main>
  </body>
</html>
